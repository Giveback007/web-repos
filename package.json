{
  "name": "web-repos",
  "version": "1.0.0",
  "main": "index.html",
  "author": "Dovydas Stirpeika",
  "private": true,
  "workspaces": [
    "exercise-counter",
    "ze-list"
  ],
  "scripts": {
    "build": "yarn run-ts build",
    "build:test": "yarn run-ts build test",
    "dev": "yarn run-ts dev",
    "run-ts": "node --unhandled-rejections=warn --enable-source-maps --trace-warnings -r esbuild-register"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.18",
    "build-dev": "^0.3.2",
    "concurrently": "^7.0.0"
  }
}
